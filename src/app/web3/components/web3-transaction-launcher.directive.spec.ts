import { Web3TransactionLauncherDirective } from './web3-transaction-launcher.directive';

describe('Web3TransactionLauncherDirective', () => {
  it('should create an instance', () => {
    const directive = new Web3TransactionLauncherDirective();
    expect(directive).toBeTruthy();
  });
});
