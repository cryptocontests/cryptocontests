contract('NewParticipations', function(accounts) {
  it("should assert true", function(done) {
    var new_participations = NewParticipations.deployed();
    assert.isTrue(true);
    done();
  });
});
